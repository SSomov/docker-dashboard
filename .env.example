# Пример файла переменных окружения для Docker Dashboard

# Использовать HTTP API Docker (по умолчанию через unix-сокет)
# DOCKER_API_HTTP=1

# Префикс для фильтрации label'ов (если потребуется)
# LABEL_PREFIX=org.example
# LABEL_PREFIX_EXCLUDE=org.example

# PORT=8080